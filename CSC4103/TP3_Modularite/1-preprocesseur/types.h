#ifndef _TYPES_H
#define _TYPES_H // Crucial pour éviter d'importer 2 fois types.h, une fois via tab.h, et l'autre fois directement

struct tableau {
  int size;
  int* values;
};

#endif /* _TYPES_H */
